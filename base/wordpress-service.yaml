apiVersion: v1
kind: Service
metadata:
  name: wordpress
  namespace: wordpress-lab
  labels:
    app: wordpress
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: wordpress
